import React,{useState}from'react';import{ArrowRight,CheckCircle2}from'lucide-react';
export default function LeadForm({title='Design your first KATHYA workflow'}){
 const[done,setDone]=useState(false),[sending,setSending]=useState(false),[error,setError]=useState('');
 const submit=async e=>{e.preventDefault();setSending(true);setError('');const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));try{const r=await fetch('/api/lead',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Unable to send your request.');setDone(true);form.reset()}catch(err){setError(err.message||'Unable to send your request. Please try again.')}finally{setSending(false)}};
 if(done)return <div className="formSuccess" role="status"><CheckCircle2/><h3>Thank you. Your request has been received.</h3><p>Our team will contact you shortly to discuss your KATHYA workflow.</p></div>;
 return <form className="leadForm" onSubmit={submit}><h3>{title}</h3><div className="formGrid"><label>Name<input required name="name" autoComplete="name" placeholder="Your name"/></label><label>Work email<input required type="email" name="email" autoComplete="email" placeholder="you@company.com"/></label><label>Company<input name="company" autoComplete="organization" placeholder="Company"/></label><label>Use case<select name="usecase"><option>AI Receptionist</option><option>Sales</option><option>Customer Support</option><option>Appointments</option><option>Customer Engagement</option><option>Custom workflow</option></select></label></div><label>What should KATHYA help complete?<textarea name="message" rows="4" placeholder="Describe the customer conversation and desired outcome."/></label><label className="hp" aria-hidden="true">Website<input name="website" tabIndex="-1" autoComplete="off"/></label>{error&&<p className="formError" role="alert">{error}</p>}<button type="submit" disabled={sending}>{sending?'Sending…':<>Request a conversation <ArrowRight size={16}/></>}</button><small>Your details are used only to respond to this request.</small></form>
}