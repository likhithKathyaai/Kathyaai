# KATHYA AI React Website

React + Vite rebuild of the KATHYA marketing site.

## Run
```bash
npm install
npm run dev
```

## Production
```bash
npm run build
```
Deploy the generated `dist/` directory to a static/Node-capable host. Do not point the current WordPress auto-deployment at this folder until a staging deployment has been tested.
